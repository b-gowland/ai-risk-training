# AI Risk Training — environment variables
# Copy this file to .env.local for local development
# NEVER commit a file containing real API keys

# Anthropic API key — required for live Claude feedback feature
# Get yours at: https://console.anthropic.com
# Note: for production, use a serverless proxy rather than exposing this in the browser
# VITE_ANTHROPIC_API_KEY=sk-ant-...
